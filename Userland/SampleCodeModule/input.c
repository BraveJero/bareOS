#include <input.h>

#define MAX_BUF_SIZE 256

void cat(void) {
//     int32_t counter = 0;
//   int64_t c;
//   while ((c = get_char()) != -1) {
//       put_char(STDOUT_FILENO, c);
//     } else {
//       if (c == '\b')
//         counter--;
//       else
//         couner++;
//       put_char(STDOUT_FILENO, c);t
//     }
    exit();
}
